# Project Instructions

Always read and follow `CODING_GUIDELINES.md` before writing any code in this project.

## Activation Check

Before implementing any task, scan the request for correctness-critical language — words like "filter", "only", "must", "ensure", "validate", "never", "always", "exactly". When present, treat them as hard constraints: after implementation, verify the output satisfies each one literally before presenting. Small tasks are not exempt.
